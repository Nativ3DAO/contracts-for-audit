{
  "name": "hardhat-project",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "lint": "hardhat lint",
    "deploy-rollupCreator": "hardhat deploy --network arbGoerliRollup --tags Rollup",
    "deploy-rollup": "hardhat deploy --network arbGoerliRollup --tags CreateRollup",
    "etherscan-verify": "hardhat etherscan-verify --network arbGoerliRollup --api-key "
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "dotenv": "^16.3.1",
    "hardhat": "^2.17.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.9.2",
    "@openzeppelin/contracts-upgradeable": "^4.9.2",
    "hardhat-deploy": "^0.11.34"
  }
}